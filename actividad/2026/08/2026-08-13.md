# Actividades - 13/08/2026

## Tareas realizadas

- Diseño y creación de tablas Firebird `SINCRONIZACIONES` y `EMPRESAS_SINCRONIZADAS`, siguiendo las convenciones del sistema (dominios `T_CLAVE_UNICA`, `T_BOOLEAN`, `T_FECHA_HORA`).
- Creación del generador + trigger `CRE_SINCRONIZACIONES` (patrón igual a `CRE_EMPRESAS`) y del procedure de inserción en `SINCRONIZACIONES` con validación de duplicados (secuencial, BaseUrl, version, key_pc, nombre_server).
- Creación de la clase C# `SincronizacionVal` (record → convertido a class) con las propiedades de la tabla.
- Lectura del contenido completo de `appsettings.json` desde código C# (usando `Startup.RootPath`).
- Implementación del método C# (versión async y luego sync) que llama al endpoint interno de validación de sincronización, enviando `SincronizacionVal` como JSON.
