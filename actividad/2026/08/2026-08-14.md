# Actividades - 14/08/2026

## Tareas realizadas

- Implementación de un script JS que consulta periódicamente el estado de sincronización, muestra un cartel con cuenta regresiva cuando el sistema va a actualizarse, y recarga automáticamente la página al llegar a cero.
- Creación de dos procedures Firebird para `SINCRONIZACIONES` — uno que trae el último registro de cada "grupo" (SECUENCIAL + BASE_URL + KEY_PC + NOMBRE_SERVER) y otro que devuelve el historial completo de un grupo a partir de un `COD_SINCRO` — junto con los endpoints C# para consumirlos.
- Configuración de WhatsApp con bot de ventas.
- Armado del registro de actividad diaria (fin-dia) de los días 12/08 y 13/08: revisión de sesiones de Claude Code y chats de claude.ai, redacción y commit/push de los archivos correspondientes.
