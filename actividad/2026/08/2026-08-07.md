# Actividades - 07/08/2026

## Tareas realizadas

- Continuación del desarrollo del bot de carga de prospectos: retomada la tarea del día anterior, quedando pendiente definir la respuesta final del flujo y el procedure de guardado.
- Creación del stub del procedure `PROC_ALTA_PROSPECTO_BOT` (parámetros: FECHA, COD_USUARIO, COD_VENDEDOR, NOMBRE, APELLIDO, EMAIL, TELEFONO, COD_CATEGORIA_MENSAJE, COD_ENTIDAD, COD_RESULTADO_RECLAMO), ajustado a las convenciones del sistema (prefijo `PROC_`, dominios `T_FECHA_HORA` y `T_CLAVE_UNICA`, agregado `SUSPEND`); queda pendiente completar la lógica real de este procedure y de `PROC_GET_USUARIO_X_SESION`.
- Estudio del circuito de "prenecesidad" (introducido por Diego) como preparación para un futuro bot de ese circuito; se revisó `PROC_CREAR_PRE_CONTRAT_DESC_MOV` en `script.sql` y se detectaron diferencias entre una firma pasada y la real en la base (4 parámetros que ya no existen).
