# Actividades - 11/08/2026

## Tareas realizadas

- Prueba del procedimiento `PROC_WS_ACTUALIZAR_COBRANZAJK4` (comprobantes tipo recibo con HABER null) de cara a una reunión con Marcelo: se revisó `PROC_WS_ACTUALIZAR_COBRANZA`, `PROC_WS_ACTUALIZAR_COBRANZA_ADI` y `PROC_SET_COBRO_COMPROB_X_COBR` en `script.sql`, sin encontrar referencia a HABER; quedó pendiente el mensaje de error exacto para seguir la traza.
- Definición de estrategia para consultar documentación del sistema: se recomendó armar un índice simple (INDEX.md) en vez de un MCP para navegar ~500 archivos .md, dado que Grep/Glob ya cubre la búsqueda full-text.
- Ayuda a Bautista con la instalación de Hammer y soporte por un error de foco en un control.
