# Actividades - 06/08/2026

## Tareas realizadas

- Cambio en JK3 de la empresa Aires de Paz: modificación de configuración de appsettings y sincronización.
- Desarrollo del bot de carga de prospectos: definición del flujo completo (paso automático inicial con `PROC_GET_USUARIO_X_SESION`, y campos del formulario con sus webservices/procedures: tipo evento, canal, resultado, fecha de agenda, grupo de prospección).
- Trabajo con Ramón Oviedo actualizando el sistema para que sea compatible con la carga masiva de comprobantes en proveedores.
- Detección y corrección en `TABLA_PRECOMPILADORES`: usaba un proyecto propio (`PRJ_ROVIEDO`) en vez de `PRJ_ARGENTINA`; corregido con Christian Jake.
- Permisos otorgados a `DLLExternas` en el servicio; no resolvió el problema de compilación de la DLL en la carpeta, se usó como workaround una DLL ya compilada.
- Reunión con Christian Jake por bug en grilla: comillas dobles sin escapar en el dato de `TIPO_COMPROBANTE` rompían el JSON (`FAC "A" COMP (SUA)`); resuelto al vuelo, causa raíz identificada (JSON armado por concatenación sin escape) y pendiente el arreglo de fondo con Jon.
- Análisis del dump completo de stored procedures (`script.sql`) del sistema para tener como referencia las convenciones (nombres, estructura, patrón RESULTADO/MENSAJE_ERROR) al armar procedures nuevos.
- Consulta técnica sobre diferencia entre `bootstrap.bundle.js` y `bootstrap.js`, y cómo separar Popper.js compatible con Bootstrap 5.3.3.

## Pendientes

- Normalizar en ABM/OrdenesCompra que Fecha, Fecha IVA y Fecha contable sean iguales a la fecha del comprobante.
- Arreglar de raíz el bug de comillas en `TIPO_COMPROBANTE` (hablar con Jon).
- Actualizar el JS de Popper — recordatorio para el lunes 10/08.
